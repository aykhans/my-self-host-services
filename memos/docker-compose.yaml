networks:
    caddy:
        name: caddy
        driver: bridge
        external: true

services:
    memos:
        image: neosmemo/memos:0.25
        container_name: memos
        volumes:
            - ./data/app:/var/opt/memos
        ports:
            - 5230:5230
        networks:
            - caddy
